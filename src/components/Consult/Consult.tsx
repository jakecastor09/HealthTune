import Container from '../Container/Container';
import ConsultHeader from './ConsultHeader';

const Consult = () => {
  return (
    <Container styles='bg-whiteMagnolia'>
      <ConsultHeader />
    </Container>
  );
};

export default Consult;
